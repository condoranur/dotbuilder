# title: setup the main ls alias with common args
# tags: ls | alias
# setup the main ls alias with common args
# we always pass these to ls(1)
LS_COMMON="-hBG"

test -n "$LS_COMMON" &&
alias ls="command ls $LS_COMMON"
