# title: username & uptime on login
# tags: login
# username & uptime on login
# requires: detect interactive shell | detect login shell
test -n "$INTERACTIVE" -a -n "$LOGIN" && {
    uname -npsr
    uptime
}
